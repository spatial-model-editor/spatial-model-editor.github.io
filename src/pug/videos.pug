extends layouts/base

block content
  include mixins/video_card
  section.pt-4
    .container.px-lg-5
      each video in json.videos
        +video_card(video.title, video.filename, video.code_url, video.colab_url)
